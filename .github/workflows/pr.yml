name: Django CI

on: [pull_request]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v3
        with:
          python-version: "3.9"

      - name: Install Poetry
        uses: abatilo/actions-poetry@v2.0.0
        with:
          poetry-version: 1.8.4  # Use the latest stable version available

      - name: Install Dependencies
        run: |
          make install && make install-pre-commit

      - name: Lint code
        run: make lint

      # Uncomment to run tests
      # - name: Run Tests
      #   run: |
      #     python manage.py test
